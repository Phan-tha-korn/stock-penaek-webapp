Data folders

- `live/` contains the SQLite database used by the app.

The app automatically copies the old legacy database from `server/db/app.db` into `live/stock-penaek.db` the first time it starts with the new layout.
