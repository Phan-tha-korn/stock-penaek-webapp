# FlowChart การทำงานของทั้งระบบ

เอกสารนี้ใช้ดูภาพรวมว่าระบบทำงานอย่างไรตั้งแต่ต้นจนจบ และข้อมูลแต่ละส่วนไหลไปทางไหน

## 1. ภาพรวมระบบทั้งหมด

```mermaid
flowchart TD
    A["ผู้ใช้เข้าสู่ระบบ"] --> B{"บทบาทผู้ใช้"}
    B -->|"OWNER"| C["Owner Zone"]
    B -->|"DEV"| D["Dev Zone"]
    B -->|"ADMIN"| E["Admin Zone"]
    B -->|"STOCK"| F["Stock Zone"]

    C --> G["ค้นหา / เทียบสินค้า"]
    D --> G
    E --> G
    F --> G

    C --> H["คิวตรวจสอบ"]
    D --> H
    E --> H

    C --> I["จัดการสินค้า / ร้านค้า / ธุรกรรม"]
    D --> I
    E --> I
    F --> I

    I --> J["ราคา / ต้นทุน / สูตรคำนวณ"]
    I --> K["ข้อมูลร้านค้า / ไฟล์แนบ"]
    I --> L["กลุ่มเทียบสินค้า Canonical Group"]

    J --> M["สร้างหรือเปลี่ยน price record"]
    K --> N["สร้างหรือแก้ supplier"]
    L --> O["move / merge / split / lock"]

    M --> P{"เป็นการเปลี่ยนแปลงสำคัญหรือไม่"}
    N --> Q{"ต้องให้ตรวจสอบหรือไม่"}
    O --> R{"มีผลกระทบที่ต้องตรวจสอบหรือไม่"}

    P -->|"ใช่"| S["สร้าง Verification Request"]
    Q -->|"ใช่"| S
    R -->|"ใช่"| S

    P -->|"ไม่ใช่"| T["อัปเดต Projection / Search"]
    Q -->|"ไม่ใช่"| T
    R -->|"ไม่ใช่"| T

    S --> U["คิวตรวจสอบอัตโนมัติ"]
    U --> V["DEV / OWNER ตรวจสอบ"]
    V --> W{"อนุมัติหรือไม่"}

    W -->|"อนุมัติ"| X["Apply Change แบบ Transaction เดียว"]
    W -->|"ไม่อนุมัติ"| Y["Reject / Return / Escalate"]

    X --> Z["สร้าง Snapshot ย้อนหลัง"]
    X --> AA["อัปเดต Search / Compare Projection"]
    X --> AB["สร้าง Notification"]
    X --> AC["เขียน Audit Log"]

    Y --> AB
    Y --> AC

    T --> AB
    T --> AC

    AB --> AD["Notification Queue"]
    AD --> AE["LINE / Email / Discord ในอนาคต"]

    Z --> AF["Historical Report / Audit Trace"]
    AA --> AG["ค้นหาอัตโนมัติ / เทียบสินค้าแบบไว / ประวัติราคา"]
```

## 2. Flow หลักที่ผู้ใช้เจอบ่อยที่สุด

### 2.1 ค้นหา → เทียบสินค้า → เปิดรายละเอียด

```mermaid
flowchart LR
    A["พิมพ์คำค้นหา"] --> B["ระบบเด้งผลลัพธ์อัตโนมัติ"]
    B --> C["เลือกสินค้าอย่างน้อย 2 รายการ"]
    C --> D["ระบบเทียบราคาให้อัตโนมัติ"]
    D --> E["สรุปอันดับราคาที่ดีที่สุด"]
    E --> F["กดดูรายละเอียดสินค้า"]
```

### 2.2 เปลี่ยนข้อมูลสำคัญ → ตรวจสอบ → อนุมัติ → แจ้งเตือน

```mermaid
flowchart LR
    A["ADMIN / OWNER / SYSTEM สร้างการเปลี่ยนแปลง"] --> B["ระบบประเมินความเสี่ยง"]
    B --> C["สร้างคำขอตรวจสอบ"]
    C --> D["คิวตรวจสอบอัตโนมัติ"]
    D --> E["DEV / OWNER เปิดตรวจ"]
    E --> F{"อนุมัติ?"}
    F -->|"ใช่"| G["Apply Change"]
    F -->|"ไม่ใช่"| H["Reject / Return / Escalate"]
    G --> I["สร้าง Snapshot"]
    G --> J["ส่ง Notification"]
    G --> K["บันทึก Audit"]
```

### 2.3 Notification → เปิดรายการที่เกี่ยวข้อง → ทำงานต่อ

```mermaid
flowchart LR
    A["Notification"] --> B["กดเปิดรายการที่เกี่ยวข้อง"]
    B --> C{"ชนิดของรายการ"}
    C -->|"Verification"| D["เปิดคิวตรวจสอบ"]
    C -->|"Pricing"| E["เปิดหน้าเทียบสินค้า / รายละเอียดสินค้า"]
    C -->|"Supplier"| F["เปิดหน้าร้านค้า"]
    D --> G["ตัดสินใจ / ดำเนินการต่อ"]
    E --> G
    F --> G
```

## 3. ระบบย่อยสำคัญเกี่ยวข้องกันอย่างไร

- `Supplier` เป็นฐานข้อมูลร้านค้าและไฟล์แนบ
- `Pricing / Cost / Formula` เป็นฐานราคาที่ใช้เทียบ
- `Matching` ทำให้สินค้าคล้ายกันถูกเทียบในกลุ่มเดียวกันได้
- `Verification` ควบคุมการเปลี่ยนแปลงสำคัญ
- `Search / Compare` ใช้ projection เพื่อให้ค้นหาและเทียบได้เร็ว
- `Notifications` ใช้แจ้งงานสำคัญและพาไปยังหน้าที่เกี่ยวข้อง
- `Snapshots` ใช้ตรึงข้อมูลย้อนหลังเพื่อรายงานและ audit

## 4. ใช้ Snapshot ตอนไหน

- ตอน approve งานสำคัญ
- ตอนเปลี่ยนราคา critical
- ตอนต้องดูรายงานย้อนหลังที่ห้ามเปลี่ยนตาม live data

## 5. ใช้ Live Data ตอนไหน

- ตอนค้นหาสินค้า
- ตอนเทียบสินค้าแบบไว
- ตอนดูคิวตรวจสอบล่าสุด
- ตอนดูการแจ้งเตือนล่าสุด

## 6. ลำดับการใช้งานจริงที่แนะนำ

1. เตรียมสินค้า / ร้านค้า / ราคา
2. จัดกลุ่มเทียบสินค้าให้ถูก
3. ใช้ค้นหาอัตโนมัติและเทียบสินค้าแบบไว
4. ถ้ามีงานสำคัญ ให้ดูที่คิวตรวจสอบอัตโนมัติ
5. อนุมัติแล้วค่อยดู snapshot / notification / audit ย้อนหลัง
